# Re-pinned for Phase 1 of the rebuild (see /home/mdeff/.claude/plans — rebuild plan).
# Dropped vs. the old root requirements.txt: fastapi, uvicorn (never used — this is a
# pure Flask app), ipfshttpclient (ai_ipfs code used raw `requests`, not this library),
# Flask-WTF/CSRF (CSRF protection is a cookie-session concern; auth here is JWT bearer
# tokens issued to a separate-origin frontend, so it doesn't apply the same way).
#
# Execution-verified 2026-08-18 against a real local backend (Python 3.11 via
# `uv python install 3.11` — the system's stock Python was too new for several
# of these exact pins to have prebuilt wheels for, and there's no compiler in
# that sandbox to build from source; a real deployment target won't have that
# problem, but if you ever need to reproduce this locally, `uv` sidesteps it).

Flask==3.0.3
Flask-CORS==4.0.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Limiter==3.8.0
SQLAlchemy==2.0.32
python-dotenv==1.0.1
psycopg2-binary==2.9.9
gunicorn==22.0.0

# Chain SDKs
web3==6.20.1
eth-account==0.11.2
solana==0.34.3
solders==0.21.0
PyNaCl==1.5.0
base58==2.1.1

# Solidity compilation (Phase 2). The old app imported `solcx` in
# smart_contract_deployer.py but never actually listed this package in
# requirements.txt — a preexisting gap, fixed here. On first use this
# downloads a solc binary (see app/services/solidity.py for why that's lazy
# now instead of happening at import time).
#
# Pinned to 2.0.5, not the original 2.0.3: found while getting a real local
# backend running — 2.0.3 hardcodes solc-bin.ethereum.org as the binary
# download host, a domain the Ethereum Foundation retired years ago (it has
# no DNS record at all anymore) in favor of binaries.soliditylang.org. Every
# compile call on 2.0.3 fails outright, regardless of environment — this
# isn't a sandbox-specific issue, it'd fail identically in production. 2.0.5
# points at the correct current host.
py-solc-x==2.0.5

# NFT Collection Generator + AI Trait Identifier (Phase 3)
Pillow==10.4.0
numpy==2.0.1
requests==2.32.3
openai==1.42.0

# Testing (Phase 7). `pytest` from backend/ picks these up via pytest.ini
# (testpaths = tests) + tests/conftest.py. Written but NOT execution-verified
# in this sandbox — same no-pip/venv limitation as everything else here.
pytest==8.3.2
