# Re-pinned for Phase 1 of the rebuild (see /home/mdeff/.claude/plans — rebuild plan).
# Dropped vs. the old root requirements.txt: fastapi, uvicorn (never used — this is a
# pure Flask app), ipfshttpclient (ai_ipfs code used raw `requests`, not this library),
# Flask-WTF/CSRF (CSRF protection is a cookie-session concern; auth here is JWT bearer
# tokens issued to a separate-origin frontend, so it doesn't apply the same way).
#
# Execution-verified 2026-08-18 against a real local backend (Python 3.11 via
# `uv python install 3.11` — the system's stock Python was too new for several
# of these exact pins to have prebuilt wheels for, and there's no compiler in
# that sandbox to build from source; a real deployment target won't have that
# problem, but if you ever need to reproduce this locally, `uv` sidesteps it).

Flask==3.1.3
Flask-CORS==6.0.5
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-JWT-Extended==4.7.4
Flask-Limiter==4.1.1
SQLAlchemy==2.0.52
python-dotenv==1.2.3
psycopg2-binary==2.9.12
gunicorn==26.2.0

# Flask-Limiter's storage backend for RATE_LIMIT_STORAGE_URI (see
# app/config.py) — a real Redis instance now exercises this in CI (see
# backend/tests/test_ratelimit_storage.py).
redis==8.1.0

# Chain SDKs
#
# web3/eth-account tried at 8.0.0/0.14.0 during a 2026-09 dependency-bump
# pass — the code-level fix was trivial (web3 renamed geth_poa_middleware to
# ExtraDataToPOAMiddleware) and real signature-verification/live-RPC/compile
# +estimate checks all passed. Reverted anyway: web3==8.0.0's async
# websocket provider requires websockets>=13, but solana==0.34.3 (below)
# has a hard `websockets<12` constraint — pip/uv CANNOT resolve both from a
# clean install (confirmed: a fresh `uv pip install -r requirements.txt`
# fails outright), even though installing them in a specific order in an
# already-populated venv appeared to work. Bumping web3 alone would ship an
# environment no clean install could reproduce.
#
# solana/solders themselves are held back for a separate, bigger reason:
# 0.40.3/0.29.0 remove solana.rpc.api's synchronous Client entirely
# (async-only now), which blockchain.py's whole Solana integration is built
# on — that's a real rewrite (async Flask routes, or wrapping every call
# site in asyncio.run), not a safe isolated bump.
#
# Bumping either now requires bumping both together as one dedicated pass
# (the Solana async rewrite, verified against the real solana-test-validator
# this environment has access to), not a routine update.
web3==6.20.1
eth-account==0.11.2
solana==0.34.3
solders==0.21.0
PyNaCl==1.6.2
base58==2.1.1

# Solidity compilation (Phase 2). The old app imported `solcx` in
# smart_contract_deployer.py but never actually listed this package in
# requirements.txt — a preexisting gap, fixed here. On first use this
# downloads a solc binary (see app/services/solidity.py for why that's lazy
# now instead of happening at import time).
#
# Pinned to 2.0.5, not the original 2.0.3: found while getting a real local
# backend running — 2.0.3 hardcodes solc-bin.ethereum.org as the binary
# download host, a domain the Ethereum Foundation retired years ago (it has
# no DNS record at all anymore) in favor of binaries.soliditylang.org. Every
# compile call on 2.0.3 fails outright, regardless of environment — this
# isn't a sandbox-specific issue, it'd fail identically in production. 2.0.5
# points at the correct current host.
py-solc-x==2.0.5

# NFT Collection Generator + AI Trait Identifier (Phase 3)
Pillow==12.3.0
numpy==2.4.6
requests==2.34.2
openai==3.13.0
# Bumped from 1.42.0 during a 2026-09 dependency-bump pass, verified against
# a real request to OpenAI's API (a deliberately-invalid key reached the
# network and got a real 401 back, not a client-side TypeError) using the
# exact call shape ai_traits.py._analyze_with_ai_vision uses (model,
# response_format, max_tokens, vision-content-array messages) — no code
# changes needed. Also resolves the httpx pin below's original problem for
# free: openai 3.x depends on its own separately-namespaced `httpx2` fork,
# not the real `httpx` package, so the version conflict this used to work
# around no longer applies to openai at all.
#
# httpx itself is still pinned — not for openai anymore, but because
# solana==0.34.3 (above) directly depends on it (httpx>=0.23.0) and this is
# a known-good version already verified working with everything else here;
# no forcing reason to touch it until solana itself gets its own dedicated
# bump pass.
httpx==0.27.2

# Testing (Phase 7). `pytest` from backend/ picks these up via pytest.ini
# (testpaths = tests) + tests/conftest.py.
pytest==9.1.1
